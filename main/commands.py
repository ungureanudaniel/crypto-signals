"""
This module is going to parse the CLI command and check if the user command in the CLI is correct, otherwise
show hints
---- under construction ----
"""
import argparse

parser = argparse.ArgumentParser(description='description')

args = parser.parse_args()
